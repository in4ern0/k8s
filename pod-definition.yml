apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    tier: front-end
    app: nginx-pod-app
spec:
  containers:
    - name: nginx-container
      image: nginx
      ports:
       - containerPort: 80
